package com.cinematica.preview.feature.authorization

import androidx.compose.runtime.Composable
import androidx.compose.ui.tooling.preview.Preview
import com.cinematica.style.system.theme.AppTheme

@Preview
@Composable
internal fun StartAuthorizationScreenPreview() {
    AppTheme {
        // AuthorizationScreen(stateMachine = fakeStateMachine(State()), onNavigateToConfirmation = {})
    }
}